name: merge-queue

on:
  status:
  pull_request:
    types:
      - labeled

jobs:
  merge-queue:
    runs-on: ubuntu-latest
    steps:
      - uses: GetResQ/resq-merge-bot@main
        with:
          checks-to-skip: 2000
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
